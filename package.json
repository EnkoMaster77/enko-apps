{
  "name": "enko-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "serve": "^14.2.0"
  },
  "scripts": {
    "start": "serve -s build",
    "build": "echo Skipping build for starter; replace with real build later"
  }
}